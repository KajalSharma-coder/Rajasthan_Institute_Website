# Rajasthan Institute Website

React + Vite website for Rajdhani Education.

## Scripts

- `npm run dev` starts the local development server.
- `npm run build` creates the production build.
- `npm run lint` runs ESLint checks.
